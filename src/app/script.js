// Espera a que el documento esté completamente cargado
document.addEventListener("DOMContentLoaded", function() {
    // Desplázate hacia arriba al cargar la página
    window.scrollTo(0, 0);
  });

  alert("¡El script de JavaScript está funcionando!");